{
  "name": "ucu-mcp",
  "version": "0.6.8",
  "description": "MCP server for Universal Computer Use — desktop automation for AI agents via Model Context Protocol",
  "type": "module",
  "bin": {
    "ucu-mcp": "dist/bin/ucu-mcp.js"
  },
  "files": [
    "native/",
    "dist/bin/",
    "dist/src/",
    "dist/index.js",
    "dist/index.d.ts",
    "skills/",
    "README.md",
    "CHANGELOG.md"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run build:native",
    "start": "node dist/bin/ucu-mcp.js",
    "dev": "tsx bin/ucu-mcp.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run tests/integration/",
    "test:macos-gui": "UCU_MACOS_GUI_SMOKE=1 vitest run tests/integration/macos-gui-smoke.test.ts",
    "test:client-cli": "UCU_CLIENT_CLI_SMOKE=1 vitest run tests/integration/client-cli-smoke.test.ts",
    "prepublishOnly": "npx vitest run tests/unit/ && npm run build",
    "build:native": "cd native/cgevent && swiftc -O -o cgevent-helper main.swift -framework CoreGraphics -framework Foundation && cd ../ocr && swiftc -O -o ocr-helper main.swift -framework Vision -framework AppKit && cd ../windowlist && swiftc -O -o windowlist-helper main.swift -framework CoreGraphics -framework Foundation && cd ../skylight && swiftc -O -o skylight-helper main.swift -framework CoreGraphics -framework Foundation -framework AppKit -framework Cocoa && cd ../sck && swiftc -O -o sck-helper main.swift -framework ScreenCaptureKit -framework CoreGraphics -framework AppKit -framework Foundation -framework ImageIO && cd ../ax && swiftc -O -o ax-helper main.swift -framework ApplicationServices -framework AppKit -framework Foundation"
  },
  "keywords": [
    "mcp",
    "computer-use",
    "desktop-automation",
    "macos",
    "accessibility",
    "ai-agent"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kaguyaluna2333/ucu-mcp-backup.git"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^4.1.7"
  }
}
